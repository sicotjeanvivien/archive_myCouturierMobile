import React, { Component } from 'react';
import { Text, View, Button } from 'react-native';

export const SplashScreen = () => {
    return (
      <View>
        <Text>Loading...</Text>
      </View>
    );
  }